@model reservations_web.Models.Room.MiniatureRoomViewModel

<div class="card">
    <div class="card-body">
        <div class="card-title">@Model.Title</div>
        <p class="card-text">@Model.ShortenedDescription</p>
        <a href="#">Book now!</a>
    </div>
</div>
